public class Ejercicio12 : MonoBehaviour
{
    List<string> inventario = new List<string>();
    public int[] numeros = { 4, 2, 4 };
    void Start()
    {
        ComparadorDeNumeros(0, 1);
        ComparadorDeNumeros(1, 2);
        ComparadorDeNumeros(0, 2);

    }
    void Update()
    {
        
    }
    public void ComparadorDeNumeros(int pos1, int pos2)
    {
        if (numeros[pos1] > numeros[pos2])
        {
            Debug.Log("Mayor: " + numeros[pos1]);
        }
        else if (numeros[pos1] < numeros[pos2])
        {
            Debug.Log("Mayor: " + numeros[pos2]);
        }
        else
        {
            Debug.Log("Son iguales");
        }
    }
}
